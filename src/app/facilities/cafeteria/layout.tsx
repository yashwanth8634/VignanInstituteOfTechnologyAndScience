export const metadata = {
    title: "Cafeteria | VITS",
    description: "Information about Cafeteria at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
