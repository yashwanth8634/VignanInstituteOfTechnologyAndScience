export const metadata = {
    title: "Computer Center | VITS",
    description: "Information about Computer Center at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
