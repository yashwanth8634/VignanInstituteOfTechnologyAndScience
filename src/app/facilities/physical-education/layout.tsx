export const metadata = {
    title: "Physical Education | VITS",
    description: "Information about Physical Education at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
