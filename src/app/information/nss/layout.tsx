export const metadata = {
    title: "NSS | VITS",
    description: "Information about NSS at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
