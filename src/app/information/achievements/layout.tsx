export const metadata = {
    title: "Achievements | VITS",
    description: "Information about Achievements at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
