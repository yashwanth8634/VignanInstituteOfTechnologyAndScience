export const metadata = {
    title: "Professional Bodies | VITS",
    description: "Information about Professional Bodies at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
