export const metadata = {
    title: "Committees | VITS",
    description: "Information about Committees at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
