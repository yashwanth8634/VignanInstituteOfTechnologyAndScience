export const metadata = {
    title: "Admissions | VITS",
    description: "Information about Admissions at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
