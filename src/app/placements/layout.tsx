export const metadata = {
    title: "Placements | VITS",
    description: "Information about Placements at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
