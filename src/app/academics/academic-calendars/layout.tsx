export const metadata = {
    title: "Academic Calendars | VITS",
    description: "Information about Academic Calendars at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
