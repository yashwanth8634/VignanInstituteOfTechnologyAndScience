export const metadata = {
    title: "Syllabus | VITS",
    description: "Information about Syllabus at VITS.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
